{% extends 'layout.html.twig' %}

{% block title %}Design & création{%  endblock %}

{% block stylesheet %}
    <link rel="stylesheet" href="/assets/css/pages/Annonce/withcards.css">
{% endblock %}

{% block content %}
    <div class="container">
        <div class="header-title">
            <h1>Design & création</h1>
        </div>
        <div class="card-container">
            {% for annonce in annonces %}
                {% include 'component/_card.html.twig'with  { annonce : annonce } %}
            {% endfor %}
            {% for myAnnonce in myAnnonces %}
                {% include 'component/_card.html.twig'with  { annonce : annonce } %}
            {% endfor %}
            {% for myAnnonce in myAnnonces %}
                {% include 'component/_card.html.twig'with  { annonce : annonce } %}
            {% endfor %}
        </div>
    </div>
{% endblock %}

{% block javascript %}
<script>
    var elements = document.getElementsByClassName("contacter");
    for (var i = 0; i < elements.length; i++) {
    elements[i].style.display = "none";
}
    // var elements = document.getElementsByClassName("button-card");
    // for (var i = 0; i < elements.length; i++) {
    // elements[i].style.display = "block";
    // }
    </script>
{% endblock %}
